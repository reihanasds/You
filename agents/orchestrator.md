# Orchestrator

Routes a concrete topic to the content strategist and keeps the output in draft-only mode. It must pass the evidence ledger to every downstream role and never turn an unknown fact into a claim.
